# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Env and secrets
.env

# OS
.DS_Store

# Live pull raw downloads (the transient source files; products stay tracked)
data/_incoming/

# Local agent worktrees
.claude/worktrees/

# Dashboard derived series (rebuilt on every ./run.sh serve, never a source of truth)
ui/series.json
